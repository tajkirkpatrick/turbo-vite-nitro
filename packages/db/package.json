{
  "name": "db",
  "version": "1.0.0",
  "scripts": {
    "generate": "drizzle-kit generate:sqlite",
    "push": "drizzle-kit push:sqlite",
    "dev": "pnpm generate & pnpm push & drizzle-kit studio",
    "build": "pnpm tsc --outDir dist -p ./src"
  },
  "dependencies": {
    "@types/better-sqlite3": "^7.6.9",
    "better-sqlite3": "^9.4.1",
    "drizzle-orm": "^0.29.3",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.20.14"
  }
}
